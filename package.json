{
  "name": "api-test",
  "version": "1.0.0",
  "description": "Postman API tests executed via Newman",
  "scripts": {
    "test": "node run_all.js"
  },
  "dependencies": {
    "newman": "latest",
    "newman-reporter-htmlextra": "latest"
  }
}